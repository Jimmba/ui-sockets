export enum SERVER_MESSAGES {
  "SHUTDOWN" = "Shutting down the server...",
}

export enum WEBSOCKET_MESSAGES {
  "CLOSED" = "WebSocket-server is closed",
  "ERROR" = "Error closing WebSocket server:",
}

export enum HTTP_MESSAGES {
  "CLOSED" = "HTTP-server is closed",
  "ERROR" = "Error closing HTTP server:",
}
